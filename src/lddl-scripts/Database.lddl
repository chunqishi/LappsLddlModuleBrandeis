/*
 * Database connection info.
 */

database='langrid'
rolename='langrid'
password='langrid'

database {
    url "jdbc:postgresql://localhost/${database}"
    username "${rolename}"
    password "${password}"
}
